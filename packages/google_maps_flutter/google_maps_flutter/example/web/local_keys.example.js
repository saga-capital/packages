// TEMPLATE FILE. This file IS committed and serves as a starter for
// `web/local_keys.js` (which is .gitignored).
//
// Setup:
//   1. Copy this file to `web/local_keys.js` in the same directory.
//   2. Replace the placeholder below with your real Google Maps JS API key.
//   3. Do NOT commit `web/local_keys.js` — it is excluded via .gitignore.
//
// The example's `web/index.html` reads `window.GMAPS_KEY` and dynamically
// injects the maps.googleapis.com script tag with it.
window.GMAPS_KEY = "YOUR_GOOGLE_MAPS_JS_API_KEY_HERE";
